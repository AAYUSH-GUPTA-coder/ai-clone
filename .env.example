# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
APP_SECRET=
# Public base URL used to build share/edit links. No trailing slash.
BASE_URL=http://localhost:8000
